# PROGRESS.md — JanarmAI MVP фронтенд

Журнал выполнения по этапам из `AGENT_BUILD_PLAN.md`. Для каждого этапа фиксируется: что сделано, как проверено, отклонения от плана.

---

## Этап 0 — Инициализация проекта

**Сделано:**
- Vite + React 18 + TypeScript (`npm create vite@latest . -- --template react-ts`, React пропинен на `^18.3.1` — в шаблоне по умолчанию был React 19, а тех.план требует React 18).
- Tailwind CSS подключён как **v4** через `@tailwindcss/vite` (CSS-first конфиг, без `tailwind.config.js`) — на момент разработки это актуальная мажорная версия в npm; поведение то же, что и у v3, синтаксис конфигурации отличается (`@theme` в CSS вместо JS-конфига). Отмечено как замена в рамках допустимых равнозначных решений (см. правила AGENT_BUILD_PLAN.md).
- `src/index.css` — токены палитры (белый / navy / orange) по разделу 8.1 ТЗ через директиву `@theme` (шкалы navy/orange 50–950, семантические токены `--color-limit-normal/warning`, `--color-status-ok/blocked`).
- `src/theme/colors.ts` — дублирование хекс-значений палитры в TS для мест, где Tailwind-классы не подходят (Recharts, react-simple-maps) — будет использовано на этапах 2 и 7.
- React Router v6+ (фактически установлен v7, обратно совместим по используемому API) с двумя независимыми корневыми деревьями:
  - `src/app/routes.tsx` + `src/app/layout/ConsumerLayout.tsx` — потребительский модуль, mobile-first контейнер (max-w-md, по центру).
  - `src/admin/routes.tsx` + `src/admin/layout/AdminLayout.tsx` — аналитический модуль, desktop-only (`min-w-[1280px]`), отдельный визуальный стиль (тёмный navy-фон, шапка «JanarmAI · Аналитика»).
  - `src/AppRouter.tsx` — корневой `BrowserRouter`, разводит `/admin/*` и `/*`.
- Заглушки (`RouteStub`) на все маршруты из карты маршрутов раздела 4 тех.плана: `/`, `/register/egov-bvu`, `/register/kmg`, `/register/kmg/foreign`, `/register/kmg/company`, `/register/kmg/company-foreign`, `/login`, `/card`, `/cabinet`, `/cabinet/history`, `/admin/login`, `/admin/dashboard` + catch-all 404 в обоих деревьях.
- Алиас `@/*` → `src/*` в `tsconfig.app.json` и `vite.config.ts`.
- Создана полная структура каталогов `src/` по разделу 3 тех.плана: `app/`, `admin/`, `features/{onboarding,auth,card,cabinet-fl,cabinet-ul,analytics}/`, `components/ui/`, `mocks/{seed,api}/`, `store/`, `theme/`, `dev/`, `types/`.

**Как проверено:**
- `npx tsc -b --noEmit` — без ошибок.
- `npm run build` — сборка проходит чисто (`dist/index.html`, `dist/assets/*`, ~5.5с).
- `npm run dev` — dev-сервер поднят на порту 5173; проверены через `curl` (HTTP 200) все 12 маршрутов из карты маршрутов, включая заведомо несуществующий (проверка catch-all).

**Отклонения от плана:**
- Tailwind CSS установился как мажорная v4 (а не v3, которая, вероятно, подразумевалась при написании тех.плана) — конфигурация через `@theme` в CSS вместо `tailwind.config.js`. Функционально эквивалентно, зафиксировано как допустимая замена.
- В процессе установки пакетов возникал конфликт из-за двух параллельных `npm install` в одной директории (повредил `node_modules`) — исправлено полной переустановкой (`rm -rf node_modules package-lock.json && npm install`).

---

## Этап 1 — Мок-слой

**Сделано:**
- `src/types/entities.ts`, `src/types/mock.ts` — модель данных мок-сущностей (тех.план раздел 7): `User`, `Company`, `Driver`, `Vehicle`, `Card`, `Transaction`, `Station`, `AdminUser`, `MockCheckResult<T>` с кодами ошибок `MockErrorCode`.
- `src/lib/cardRules.ts` — единое бизнес-правило выпуска карт при регистрации (используется и в seed, и позже в Этапе 3): для ФЛ-резидента личный лимит легковой категории (100 л) не дублируется на человека независимо от числа легковых ТС, на каждый грузовик — отдельная карта (300 л/ТС); для ЮЛ лимит всегда «на ТС»; для иностранца/ЮЛ-нерезидента — карта без суточного потолка (`dailyLimitL: null`), без льготной цены. Допущение по неоднозначности ТЗ зафиксировано в `OPEN_QUESTIONS.md`.
- `src/lib/{id,time,mask}.ts` — генерация id/QR-токенов, расчёт ближайшей полуночи по времени Астаны (сброс лимита), маскирование ИИН/БИН.
- `src/mocks/seed/*.ts` — стартовые данные: 5 пользователей (по одному на каждую ветку регистрации/входа: ФЛ-резидент eGov, ФЛ-резидент КМГ, ФЛ-иностранец, директор ЮЛ-резидента, директор ЮЛ-нерезидента), 2 компании, 8 ТС, 3 водителя, справочник лимитов/цен (100/300 л, мок-цены льготная/предельная по видам топлива), 15 АЗС по областям РК (с пометкой приграничных с РФ регионов — под будущую тепловую карту Этапа 7), 3 служебных аккаунта аналитиков, карты (сгенерированы через `cardRules` из seed-пользователей/ТС/компаний), стартовая история заправок (11 транзакций).
- `src/mocks/api/*.ts` — имитация проверок: `checkGbdFl`, `checkBmg`, `sendSmsCode`/`verifySmsCode` (identity.ts), `checkBerkut`/`verifyLiveness` (foreigner.ts), `checkVehicleRegistry`/`checkOgpoInsurance` (vehicle.ts, ЕСБД/ОГПО), `checkGbdUl`/`signEcp` (company.ts), `checkAdminCredentials` (admin.ts). Общий формат ответа `{status:'success', data}` / `{status:'error', errorCode, message}` через `mockSuccess`/`mockError` (`mockRequest.ts`), задержка 300–1500 мс (`config.ts`). Детерминированные сценарии ошибок — по признаку ИИН/БИН/паспорта/ГРНЗ, правило зафиксировано в `OPEN_QUESTIONS.md`.
- `src/store/*.ts` — 4 Zustand-стора с `persist` в localStorage: `user.store.ts` (пользователь/сессия + рантайм-база users/companies/vehicles/drivers, пополняемая при регистрации), `card.store.ts` (карты и лимиты: `consumeLimit`, `resetDaily`, `regenerateQrToken`), `transaction.store.ts` (история), `admin.store.ts` (админ-сессия). `resetDemoData.ts` — сброс всех сторов к seed по кнопке «Сбросить демо-данные» (будет подключена в UI на Этапе 6/8).
- `src/dev/MocksDevPage.tsx` на маршруте `/dev/mocks` — визуальная проверка мок-вызовов и состояния сторов вне бизнес-потоков (по аналогии с `/dev/components` из Этапа 2).
- `scripts/smoke-mocks.ts`, `scripts/smoke-persist.ts` + npm-скрипты `smoke:mocks`/`smoke:persist` (через `tsx`) — дымовые скрипты для проверки мок-слоя и persist-слоя из консоли без браузера.

**Как проверено:**
- `npx tsc -b --noEmit` и `npm run build` — без ошибок.
- `npm run smoke:mocks` — все 10 проверок из консоли вернули корректный `{status, data|errorCode, message}`, включая все запланированные негативные сценарии (ГБД ФЛ не найден/несовпадение, БМГ, SMS неверный код, «Беркут» дубликат, ИС «Автомобиль» не найдено, вход администратора неверный); суммарная задержка 11 вызовов ≈9с, что соответствует диапазону 300–1500мс на вызов.
- `npm run smoke:persist` — с полифиллом `localStorage` в Node подтверждено: (1) `consumeLimit` корректно обновляет карту в сторе, (2) обновлённое состояние физически записывается в `localStorage` под ключами `janarmai-card-store`/`janarmai-admin-store` (проверено содержимое сериализованной записи), (3) `reset()` возвращает сторы к seed-состоянию. Это подтверждает контракт persist-слоя, лежащий в основе требования «состояние переживает перезагрузку страницы» — сам факт перезагрузки браузера дополнительно не тестировался (headless-браузер недоступен в этом окружении), библиotека zustand/persist общепринята и стабильна.
- Dev-сервер (`npm run dev`) поднят, маршрут `/dev/mocks` отдаёт 200.

**Отклонения/примечания:**
- Установка `tsx` (как devDependency для дымовых скриптов) подтянула транзитивную уязвимость high severity (ReDoS в `d3-color` через `react-simple-maps`, `npm audit`). Не исправлено намеренно: fix ломает `react-simple-maps` до v1 (несовместимо с тех.планом, который явно требует эту библиотеку для карты РК), а сама уязвимость не эксплуатируема в контексте демо со статичными встроенными данными (нет обработки пользовательского ввода через уязвимый регэксп).

---

## Этап 2 — Базовая компонентная библиотека

**Сделано:**
- `src/components/ui/CardMunai.tsx` — карточка MunaiCard (тёмно-синяя, wallet-стиль, скруглённые углы, тень — ТЗ 8.2): ФИО/название, маскированный ИИН/БИН, подпись типа карты, встроенный QR (`qrcode.react`, `QRCodeSVG`) и остаток лимита крупным оранжевым текстом. Поддерживает `remainingLabel="без лимита"` для карт иностранцев/ЮЛ-нерезидентов.
- `src/components/ui/LimitProgressBar.tsx` — прогресс-бар с цветовой семантикой (синий — норма, оранжевый — приближение/превышение порога 80%, ТЗ 8.3) и отдельным режимом для карт без потолка (`limitL: null`, см. `OPEN_QUESTIONS.md`).
- `src/components/ui/StepperStatus.tsx` — пошаговый статус проверки (pending/in_progress/success/error) с иконками lucide-react в цветных круглых подложках (паттерн 8.1).
- `src/components/ui/OtpInput.tsx` — 6-ячеечный ввод SMS-кода с автопереходом фокуса и поддержкой вставки/backspace.
- `src/components/ui/{toastStore.ts,ToastNotification.tsx}` — глобальная система toast-уведомлений: `useToastStore` (Zustand, без persist — эфемерное UI-состояние) + `showToast()` + `<ToastViewport/>`, смонтирован один раз в `ConsumerLayout`.
- `src/components/ui/ChannelCard.tsx` — карточка-кнопка выбора канала (S-00).
- `src/dev/ComponentsDevPage.tsx` на маршруте `/dev/components` — все 6 компонентов с несколькими состояниями каждого (в т.ч. предупреждающий/безлимитный режим прогресс-бара, состояние ошибки степпера, отключённая ChannelCard).

**Как проверено:**
- `npx tsc -b --noEmit`, `npm run build`, `npm run lint` (oxlint) — без ошибок и предупреждений.
- Проверено содержимое собранного CSS (`dist/assets/*.css`) на предмет корректной генерации использованных Tailwind-утилит (`bg-gradient-to-br`, `grayscale`, кастомные токены `bg-status-ok`/`bg-status-blocked` из `@theme`) — присутствуют.
- Dev-сервер поднят, маршрут `/dev/components` отдаёт HTTP 200.
- **Ограничение окружения:** headless-браузер (chromium-cli/Playwright) недоступен в этой среде — визуальная проверка «компоненты видны и корректно выглядят на мобильной ширине» не подтверждена скриншотом, только статически (typecheck/build/CSS-генерация + ручной аудит используемых Tailwind-классов). Рекомендуется визуально проверить `/dev/components` в реальном браузере на ширине 375–428px при первой возможности.

---

## Этап 3 — Онбординг и регистрация (S-00…S-14)

**Сделано:**
- `src/lib/demoIdentity.ts` — генерация случайной «чистой» демо-личности (ФИО/ИИН/БИН/телефон, не попадающей под коды ошибок) для быстрых веток, где данные считаются «уже подтверждёнными каналом».
- `src/lib/cardRules.ts` дополнен `materializeCard()` — материализация карты из спецификации при выпуске в реальном времени (в отличие от `cards.seed.ts`, где id детерминированные для связки с seed-транзакциями).
- `src/features/onboarding/registrationActions.ts` — `finalizeFlRegistration()` и `finalizeUlRegistration()`: единая точка записи в сторы (`registerUser`/`registerCompany`/`addVehicle`/`addDriver`/`addCard`) по итогам любой из 5 веток; возвращают именно выпущенные карты (важно — карта грузового ТС привязана к `vehicleId`, а не к `userId`, поэтому страницы не полагаются на фильтрацию стора по одному полю).
- `src/features/onboarding/steps/*` — 12 переиспользуемых шаг-компонентов визарда: `WizardShell` (каркас с прогрессом), `ConsentStep` (S-01), `SmsStep` (S-02, общий для всех сценариев с SMS), `IinPhoneFormStep` (S-04, react-hook-form+zod), `GbdFlBmgStep` (S-05, последовательный степпер ГБД ФЛ→БМГ), `PassportLivenessStep` (S-06), `BerkutStep` (S-07, с обработкой отказа), `VehicleCheckStep` (S-08, развилка «своё ТС»/ОГПО), `LimitResultStep` (S-09), `CardIssueStep` (S-10), `EcpBinStep` (S-11), `GbdUlStep`/`ManualCompanyStep` (S-12 резидент/нерезидент), `VehicleFleetStep` (S-13, повторяемая форма автопарка), `DriverAssignStep` (S-14, опционально, имитация ГБД ФЛ на водителя).
- 6 страниц-визардов в `src/features/onboarding/`: `ChannelSelectPage` (S-00, маршрут `/`), `EgovBvuRegisterPage` (`/register/egov-bvu`, быстрый путь 4.1), `KmgRegisterPage` (`/register/kmg`, S-03 + полный путь ФЛ-резидента инлайн, 4.2), `ForeignRegisterPage` (`/register/kmg/foreign`, 4.3, с экраном «Отказ в регистрации» при дубликате паспорта), `CompanyRegisterPage` (`/register/kmg/company`, 4.4), `CompanyForeignRegisterPage` (`/register/kmg/company-foreign`, 4.5, переиспользует `PassportLivenessStep`+`BerkutStep` для верификации директора — как явно предписано AGENT_BUILD_PLAN.md).
- Дополнительный пакет `@hookform/resolvers` (связка react-hook-form + zod) — не был явно указан в тех.плане, но необходим как стандартный мост между этими двумя библиотеками из стека; отмечено здесь как техническое дополнение.
- Два допущения по неоднозначностям ТЗ зафиксированы в `OPEN_QUESTIONS.md` (одно ТС за проход S-08 у ФЛ; отсутствие отдельной ГБД ФЛ-проверки представителя ЮЛ-резидента).

**Как проверено:**
- `npx tsc -b --noEmit`, `npm run build`, `npm run lint` (oxlint) — без ошибок и предупреждений.
- `scripts/smoke-registration.ts` (`npm run smoke:registration`) — программно пройдены все 5 веток регистрации от начала до выпуска карты через ту же бизнес-логику (`registrationActions.ts`), что использует UI, включая три негативных сценария (ГБД ФЛ «не найден», БМГ «телефон не привязан», «Беркут» дубликат паспорта) и проверку итоговых параметров каждой карты (тип, лимит, признак льготной цены, привязка к пользователю/ТС). Все 5 веток и все ассерты — успешно.
- В процессе написания смок-теста обнаружен и исправлен реальный баг: `CardIssueStep` на финальном экране регистрации получал список карт фильтрацией `cards.filter(c => c.userId === userId)`, что не находило карты грузовых ТС (они привязаны к `vehicleId`, не к `userId`) — экран выпуска карты показывал бы 0 карт для ФЛ-резидента с грузовым ТС. Исправлено: `finalizeFlRegistration`/`finalizeUlRegistration` теперь возвращают сами выпущенные карты напрямую, страницы больше не полагаются на повторную фильтрацию стора.
- Dev-сервер поднят, все 6 маршрутов регистрации + `/login` + `/cabinet` отдают HTTP 200.
- **Ограничение окружения:** как и на Этапе 2, headless-браузер недоступен — интерактивное прохождение форм (клики, ввод в react-hook-form поля, переходы между шагами визарда) не подтверждено скриншотами/кликами в реальном браузере, только типобезопасностью, сборкой и прогоном бизнес-логики через смок-скрипт. Рекомендуется пройти все 5 веток вручную в браузере при первой возможности, включая позитивные и негативные сценарии, указанные в подсказках на экранах паспорта/ГРНЗ (значения `DUP`/`NF`/`ERR` и ИИН на 6-9).

**Отклонения/примечания:**
- Отдельная (гоночная) причина падения сборки в процессе работы: `npm install @hookform/resolvers` по ошибке был выполнен в родительской директории репозитория (`kmg/`), а не в `janarmai-front/` — создал паразитный `node_modules`/`package.json` на уровень выше, из-за чего сборка резолвила несовместимые версии `zod`. Обнаружено по ошибке Vite/Rolldown, исправлено удалением паразитных файлов и переустановкой пакета в правильной директории.

---

## Этап 4 — Авторизация (S-15…S-17, A-00)

**Сделано:**
- `src/features/auth/LoginPage.tsx` на маршруте `/login` — единый визард входа с выбором способа (аналог S-00, но для входа), три ветки: eGov/БВУ — выбор из списка ранее зарегистрированных аккаунтов этих каналов с почти мгновенным входом (S-15); телефон + переиспользованный `SmsStep` — резидент/иностранец через приложение КМГ (S-16); БИН/рег.номер + код подтверждения (переиспользован паттерн демо-кода `1234`) — ЮЛ резидент/нерезидент (S-17). Поиск аккаунта — по моковым учётным записям в `useUserStore` (и seed, и зарегистрированные в Этапе 3 в течение той же демо-сессии), без обращения к бэкенду, как и предписано ТЗ 4.6.
- `src/features/auth/AdminLoginPage.tsx` на маршруте `/admin/login` — отдельная landing-страница служебного входа (визуально отделена от `AdminLayout`: не использует его шапку/хром, вынесена из-под layout-обёртки в `src/admin/routes.tsx`, чтобы не дублировать бренд-заголовок), логин+пароль, `checkAdminCredentials`, стандартное сообщение об ошибке при неверных данных (ТЗ 4.7).
- Временные session-заглушки (будут полностью заменены на Этапах 6/7, но уже дают функциональный «Выйти» для сквозной проверки повторного входа): `src/features/cabinet-fl/CabinetPlaceholderPage.tsx` (`/cabinet`) показывает текущего пользователя/компанию и кнопку выхода; `src/features/analytics/AdminDashboardPlaceholderPage.tsx` (`/admin/dashboard`) — то же для админ-сессии.
- `scripts/smoke-auth.ts` (`npm run smoke:auth`) — дымовой прогон всех 4 способов входа/выхода на seed-аккаунтах + оба сценария входа администратора (успех/неверный пароль).

**Как проверено:**
- `npx tsc -b --noEmit`, `npm run build`, `npm run lint` — без ошибок и предупреждений.
- `npm run smoke:auth` — пройдены: вход/выход eGov (S-15), вход/выход КМГ телефон+SMS для резидента и для иностранца (S-16), вход/выход ЮЛ-резидента по БИН и ЮЛ-нерезидента по рег.номеру (S-17, оба случая корректно устанавливают `currentCompanyId`), вход администратора с верными данными + отклонение неверного пароля + выход (A-00). Все ассерты успешны.
- Dev-сервер поднят, `/login`, `/cabinet`, `/admin/login`, `/admin/dashboard` отдают HTTP 200.
- **Ограничение окружения:** как и на предыдущих этапах, headless-браузер недоступен — интерактивная проверка кликов по способам входа и заполнения форм не подтверждена скриншотами, только бизнес-логикой через смок-скрипт и статической проверкой (typecheck/build).

**Отклонения/примечания:**
- Поисковая логика (сопоставление телефона/БИН с пользователем/компанией) реализована непосредственно в `LoginPage.tsx`, а не вынесена в отдельный `authActions.ts` по аналогии с `registrationActions.ts` — в отличие от регистрации, вход не создаёт новых сущностей в сторах (только читает и переключает сессию), поэтому выделять отдельный слой действий сочтено избыточным для объёма логики.

---

## Этап 5 — Карта, QR и симуляция заправки (S-18…S-21)

**Сделано:**
- `src/components/ui/CardMunai.tsx` дополнен резервным цифровым кодом (8.2) — форматированное отображение части `qrToken` группами по 4 символа под QR-блоком.
- `src/features/card/fuelingActions.ts` — `simulateFueling()`: единая точка бизнес-логики завершения заправки (расчёт `priceType` по остатку льготного лимита на момент операции, `consumeLimit` в сторе карт, создание и сохранение `Transaction` со случайной АЗС из seed-списка, расчёт нового остатка и признака достижения порога 80%). Ключевое допущение (единый `priceType` на транзакцию, а не разбиение объёма) зафиксировано в `OPEN_QUESTIONS.md`.
- `src/features/card/FuelingParamsStep.tsx` (S-19) — выбор вида топлива и объёма с быстрыми пресетами и подсказкой, как продемонстрировать сверхлимитный сценарий.
- `src/features/card/FuelingProcessStep.tsx` (S-20) — степпер «Проверка лимита → Резерв объёма → Отпуск подтверждён» с задержками (укладывается в «< 2 секунды» из ТЗ), вызывает `simulateFueling` на финальном шаге.
- `src/features/card/CardScreenPage.tsx` (S-18) на маршруте `/card` — определяет сессию (ФЛ или ЮЛ) и собирает все актуальные карты пользователя/компании (включая грузовые карты, привязанные к ТС, а не к пользователю напрямую — учтён баг, найденный на Этапе 3), переключатель карт при их количестве >1, живой QR с автообновлением токена каждые 30 с (`setInterval` + `regenerateQrToken`), `LimitProgressBar`, кнопка «Симулировать заправку», запускающая S-19→S-20, и toast-уведомления (S-21) — об успешной операции и, при достижении 80% лимита, отдельное предупреждение (переиспользован `showToast` из Этапа 2).
- Ссылка «Карта / QR» добавлена в session-заглушку `/cabinet` для сквозной проверки перехода между экранами.

**Как проверено:**
- `npx tsc -b --noEmit`, `npm run build`, `npm run lint` — без ошибок и предупреждений.
- `scripts/smoke-fueling.ts` (`npm run smoke:fueling`) — прогнаны 4 сценария на прямом вызове `simulateFueling`: (1) заправка в пределах лимита — льготная цена, корректный остаток; (2) заправка, доводящая использование до 85% — предупреждение о 80% срабатывает, ещё в пределах остатка — льготная цена; (3) заправка сверх оставшегося лимита (15 л остатка, заправлено 30 л) — вся транзакция по предельной цене, `usedTodayL` увеличивается на весь объём (115 л), остаток не уходит в минус; (4) заправка карты без лимита (иностранец) — всегда по предельной цене, `remaining=null`. Также проверено, что все 4 транзакции физически добавлены в историю стора. Все ассерты успешны.
- Dev-сервер поднят, `/card` отдаёт HTTP 200.
- **Ограничение окружения:** как и на предыдущих этапах, headless-браузер недоступен — визуальная проверка живого QR-таймера, переключения карт и toast-уведомлений в браузере не проводилась, только через бизнес-логику (смок-скрипт) и статическую проверку сборки.

---

## Этап 6 — Личный кабинет ФЛ и ЮЛ (S-22…S-27)

**Сделано:**
- `src/lib/sessionCards.ts` — общий селектор карт текущей сессии (ФЛ/ЮЛ), вынесенный из `CardScreenPage.tsx` (Этап 5) для переиспользования в кабинетах без повторения логики, ранее содержавшей баг с грузовыми картами (Этап 3). `CardScreenPage.tsx` отрефакторен на использование этого хелпера.
- `src/lib/cardLabels.ts` — вынесенный общий словарь подписей типов карт (ранее дублировался в `CardIssueStep.tsx` и `CardScreenPage.tsx`).
- `src/components/ui/TransactionRow.tsx`, `src/components/ui/FilterBar.tsx` — переиспользуемые компоненты из раздела 6 тех.плана, реализованные на этом этапе (были из списка компонентов, но не требовались раньше S-23/S-25).
- `src/features/cabinet-fl/CabinetFlPage.tsx` (S-22) — карта(ы) пользователя с прогресс-баром лимита по каждой, последние 5 заправок, переход к полной истории и на экран карты.
- `src/features/cabinet-ul/CabinetUlPage.tsx` (S-24) — сводная таблица автопарка (ГРНЗ, категория, водитель, прогресс-бар лимита по ТС), встроенные S-26 (добавление ТС с автоматическим выпуском карты через `fleetActions.ts`, деактивация ТС, назначение/снятие водителя) и S-27 (кнопка «Скачать отчёт»).
- `src/features/cabinet-ul/fleetActions.ts` — `addFleetVehicle`, `deactivateVehicle`, `assignDriver`, `unassignDriver`: та же схема выпуска карты (`cardRules.materializeCard`), что и при регистрации ЮЛ (Этап 3), для новых ТС, добавляемых уже после регистрации.
- `src/features/cabinet-ul/exportReport.ts` — `exportFleetReportCsv()`: генерация CSV (через `file-saver`) со всей историей автопарка (дата, ГРНЗ, категория, водитель, АЗС, топливо, литры, тип цены, цена/л, сумма), с BOM для корректного отображения кириллицы в Excel.
- `src/features/cabinet-fl/TransactionHistoryPage.tsx` (S-23/S-25) — единая страница `/cabinet/history`, адаптирующаяся под тип сессии: для ФЛ — фильтры период/карта/АЗС/топливо; для ЮЛ — те же плюс фильтр по водителю, с отображением ГРНЗ+водителя в каждой строке истории.
- `src/app/CabinetRouterPage.tsx` — диспетчер `/cabinet`, показывающий `CabinetFlPage` или `CabinetUlPage` по наличию `currentCompanyId` в сессии. Временная заглушка `CabinetPlaceholderPage.tsx` из Этапа 4 удалена как полностью замещённая.

**Как проверено:**
- `npx tsc -b --noEmit`, `npm run build`, `npm run lint` — без ошибок; одно предупреждение `react-hooks/exhaustive-deps` (нестабильная ссылка `companyVehicles`/`companyDrivers` в `useMemo`) найдено и исправлено обёртыванием в собственный `useMemo`.
- `scripts/smoke-cabinet.ts` (`npm run smoke:cabinet`) — проверено: (1) `selectSessionCards` возвращает 2 карты для ФЛ с легковым+грузовым ТС; (2) 3 карты для автопарка ЮЛ-резидента из seed; (3) `addFleetVehicle` создаёт ТС и карту 100 л/легковая корректно, счётчик карт сессии увеличивается до 4; (4) `assignDriver`/`unassignDriver` корректно назначают и снимают водителя (водитель деактивируется, а не удаляется); (5) `deactivateVehicle` убирает ТС из сессионной выборки карт (карта становится неактивной). Все ассерты успешны.
- Dev-сервер поднят, `/cabinet` и `/cabinet/history` отдают HTTP 200.
- **Ограничение окружения:** как и на предыдущих этапах, headless-браузер недоступен — визуальная проверка таблицы автопарка, фильтров истории и фактического скачивания CSV-файла в браузере не проводилась, только через бизнес-логику (смок-скрипт) и статическую проверку сборки.

**Отклонения/примечания:**
- Оба допущения (формат/состав экспорта отчёта; встраивание S-26/S-27 в `/cabinet` вместо отдельных маршрутов) зафиксированы в `OPEN_QUESTIONS.md`.

---

## Этап 7 — Аналитический дашборд (A-01…A-06)

**Сделано:**
- Вызван скилл `dataviz` перед написанием графиков (по системным инструкциям). Категориальная пара для графиков `#3d63a0`/`#e05f0a` подобрана и валидирована через `validate_palette.js` (все 5 проверок PASS в light и dark режимах против фактических фонов приложения — белого и `navy-950`); последовательная шкала `chartSequentialNavy` (5 шагов, один тон) валидирована как ordinal-рамп для тепловой карты. Обе палитры зафиксированы в `src/theme/colors.ts`.
- **Исправление регрессии из Этапа 0:** `AdminLayout` использовал сплошной тёмный фон на весь аналитический модуль, что противоречит ТЗ 8.1 («белый — базовый фон контента ... аналитический модуль»). Исправлено: белый/светлый фон контента, navy — только в шапке. См. `OPEN_QUESTIONS.md`.
- `src/mocks/geo/kz-oblasts.json` — 14 областей РК (GeoJSON), скачан из публичного репозитория `open-data-kazakhstan/geo-boundaries-kz`, отфильтрован от подмешанных district-записей, названия переведены на русский, координаты округлены для компактности (188 КБ). Ограничения датасета (деление до реформы 2022 года) зафиксированы в `OPEN_QUESTIONS.md`.
- `src/mocks/seed/analytics.seed.ts` — сгенерированные наборы данных с детерминированной сезонностью (формула синусоиды + фиксированные аномалии, не `Math.random()`, как явно требует ТЗ раздел 6): помесячная сверка «СНТ vs Продажи» с растущим индексом легальности (78%→94%), структура потребления резиденты/нерезиденты с растущей долей нерезидентов, рейтинг 3 сетей АЗС (сеть «Гелиос» — демонстрационная аномалия: чеки ОФД выше авторизаций JanarmAI), индекс топливного туризма (приграничные регионы 21.4% vs внутренние 3.8%), потребление/аномалии по 14 областям, 5 правил интеллектуального контроля (2 «легально» / 3 «блокировка»), KPI (объём реализации и предотвращённый вывоз — по образцу цифр из самого ТЗ 8.5: «50 млн л» / «15 млн л»).
- Новые переиспользуемые компоненты (тех.план раздел 6): `KpiTile`, `ChartLine`/`ChartBar` (обобщённые обёртки над Recharts с фирменной палитрой, легендой при ≥2 сериях, тултипом), `ChartDonut`, `KzHeatMap` (react-simple-maps, заливка по индексу потребления через `chartSequentialNavy`, hover-панель с деталями региона вместо плавающего тултипа), `RuleStatusCard` (статус + иконка + подпись, не только цвет — по правилу dataviz «status is fixed... always icon+label»).
- `src/features/analytics/AdminDashboardPage.tsx` на маршруте `/admin/dashboard` — все 6 блоков ТЗ раздела 6 плюс лента источников данных внизу (СНТ/ОФД/МВД/«Беркут»/ЕСБД → ядро JanarmAI → BI-витрина). Временная заглушка `AdminDashboardPlaceholderPage.tsx` из Этапа 4 удалена как полностью замещённая.

**Как проверено:**
- `npx tsc -b --noEmit`, `npm run build`, `npm run lint` — без ошибок. Возникла типовая несовместимость между дженерик-обёрткой `ChartLine`/`ChartBar<T>` и внутренним условным типом Recharts `TypedDataKey` — устранена точечным `as never` на границе с библиотекой (публичный API обёрток остаётся строго типизированным `keyof T & string`).
- `scripts/smoke-analytics.ts` (`npm run smoke:analytics`) — проверено: (1) названия регионов в seed и в GeoJSON совпадают 1:1 (14/14, иначе тепловая карта не подсвечивала бы часть областей); (2) продажи никогда не превышают физический закуп по СНТ; (3) индекс легальности рынка растёт к концу периода; (4) доли резидентов/нерезидентов суммируются в 100% в каждом месяце; (5) сеть «Гелиос» действительно показывает демонстрационную аномалию (чеки ОФД > авторизаций); (6) доля нерезидентов в приграничных регионах выше, чем во внутренних; (7) блок контроля содержит оба статуса. Все ассерты успешны.
- Dev-сервер поднят, `/admin/login` и `/admin/dashboard` отдают HTTP 200.
- **Ограничение окружения:** headless-браузер недоступен — визуальная проверка компоновки виджетов, корректности масштаба/центра карты (`ComposableMap`) и фактического рендера SVG-полигонов не проводилась. Рекомендуется в первую очередь визуально проверить `/admin/dashboard` на ширине ≥1280px и при необходимости скорректировать `scale`/`center` тепловой карты.

**Отклонения/примечания:**
- Размер JS-бандла вырос до ~1.1 МБ (до сжатия) после добавления Recharts + react-simple-maps/d3 — Vite предупреждает о превышении 500 КБ на чанк. Код-сплиттинг/динамические импорты не применялись — вне охвата AGENT_BUILD_PLAN.md для демо-MVP; при необходимости оптимизации это отдельная задача (например, ленивая загрузка `/admin` дерева).
- Три допущения (актуальность геоданных, точность проекции карты, выбор отдельной валидированной пары цветов для графиков) зафиксированы в `OPEN_QUESTIONS.md`.

---

## Этап 8 — Сквозное QA

**Сделано:**
- Добавлена отсутствовавшая кнопка «Сбросить демо-данные» в реальном UI (тех.план 2.2: «в настройках/футере») — раньше была доступна только на служебной странице `/dev/mocks`. Теперь: футер `ConsumerLayout` (потребительский модуль, на каждом экране) и шапка `AdminLayout` (аналитический модуль). Обе вызывают `resetDemoData()` и возвращают на стартовый экран своего модуля (`/` и `/admin/login` соответственно).
- Ширина мобильного контейнера `ConsumerLayout` сужена с `max-w-md` (448px) до точных `max-w-[428px]` — верхняя граница диапазона 375–428px из тех.плана раздела 8 (нижняя граница 375px не требует отдельного CSS-ограничения, так как раскладка и так рассчитана на контент минимум с этой ширины). Ширина `AdminLayout` (`min-w-[1280px]`) не менялась — задана ещё в Этапе 0 и соответствует тех.плану без изменений.
- `scripts/smoke-reset.ts` (`npm run smoke:reset`) — сквозной прогон: мутации во всех 4 сторах одновременно (регистрация нового ФЛ, вход, симуляция заправки, вход администратора) → `resetDemoData()` → проверка, что все счётчики (`users`, `companies`, `vehicles`, `drivers`, `cards`, `transactions`) и обе сессии (`currentUserId`/`currentCompanyId`, `currentAdminId`) точно возвращаются к seed-состоянию.
- `npm run smoke:all` — сводный запуск всех 8 дымовых скриптов проекта (mocks, persist, registration, auth, fueling, cabinet, analytics, reset) одной командой для регрессионной проверки.

**Как проверено (сквозной прогон):**
- **Финальная регрессия всех дымовых скриптов** (`smoke:mocks`, `smoke:persist`, `smoke:registration`, `smoke:auth`, `smoke:fueling`, `smoke:cabinet`, `smoke:analytics`, `smoke:reset`) — все 8 прогнаны заново после изменений Этапа 8, все успешны. Суммарно покрывают: 5/5 веток регистрации (включая негативные сценарии ГБД ФЛ/БМГ/«Беркут»), 4/4 способа входа + вход администратора (успех/отказ), 4 сценария симуляции заправки (в лимите/80%/сверх лимита/безлимитная карта), кабинет ФЛ и ЮЛ с управлением автопарком, консистентность всех данных аналитического дашборда, полный сброс состояния по всем 4 сторам.
- **Полный обход маршрутов** (`curl` по HTTP, дев-сервер) — все 17 маршрутов обоих деревьев (потребитель: `/`, 5 веток регистрации, `/login`, `/card`, `/cabinet`, `/cabinet/history`, `/dev/mocks`, `/dev/components`, несуществующий путь; админка: `/admin`, `/admin/login`, `/admin/dashboard`, несуществующий путь) отдают HTTP 200 — SPA-роутинг обрабатывает неизвестные пути клиентским catch-all (S-404) корректно.
- **Адаптивность** — `npx tsc -b --noEmit`, `npm run build`, `npm run lint` без ошибок; в собранном CSS подтверждено физическое присутствие обеих граничных утилит (`428px` для потребителя, `1280px` для админки).
- **Итоговый набор итераций по этапам** (см. записи выше по каждому этапу) — все 8 этапов (0–7) собираются и запускаются без ошибок, что и было условием перехода между ними согласно AGENT_BUILD_PLAN.md.

**Известные ограничения демо (сведены воедино из записей по этапам, не новые находки):**
- **Headless-браузер недоступен во всём окружении этой сессии** — ни разу не удалось получить визуальное подтверждение (скриншот) интерфейса ни на одном этапе. Вся верификация опиралась на: типобезопасность (`tsc`), успешную сборку (`vite build`), статический анализ (`oxlint`), присутствие ожидаемых CSS-классов в собранном бандле и — что наиболее содержательно — воспроизведение бизнес-логики через 8 дымовых Node-скриптов, вызывающих ту же логику (`registrationActions`, `fuelingActions`, `fleetActions`, сторы), что и UI-компоненты. **Настоятельно рекомендуется перед демонстрацией заказчику пройти вручную в реальном браузере**: все 5 веток регистрации (включая триггеры негативных сценариев — ИИН/ГРНЗ/паспорт с подсказанными в UI признаками), все 4 способа входа, симуляцию заправки с сверхлимитным сценарием, оба кабинета, дашборд на ширине ≥1280px и мобильный поток на 375–428px.
- Все допущения по неоднозначностям ТЗ собраны в `OPEN_QUESTIONS.md` (по этапам 1, 3, 5, 6, 7) — рекомендуется согласовать с заказчиком/ПМ перед сдачей.
- Известная уязвимость транзитивной зависимости (`d3-color` через `react-simple-maps`) и рост размера бандла (~1.1 МБ) — оба сознательно приняты как некритичные для демо-MVP, см. записи Этапов 1 и 7.

---

## Постфактум-правка Этапа 7 — актуализация геоданных тепловой карты

По запросу заменён устаревший (до-2022) GeoJSON на актуальное деление 2024 года (20 регионов):
- Источник: `geokz` (github.com/arodionoff/geokz, CC BY 4.0) → слой `kaz_admbnda_adm1_2024`, основан на UN OCHA COD-AB Kazakhstan.
- Пайплайн: скачан шейпфайл (5 файлов), сконвертирован и упрощён через `mapshaper -simplify 5% -o precision=0.001` (npx, из-за отсутствия GDAL в окружении) в `src/mocks/geo/kz-oblasts.json` — **32 КБ** (было 188 КБ на 14 регионах). Поле `name` = авторитетное `ADM1_RU` из источника, единственная ручная правка — «Чимкент» → «Шымкент» (актуальное официальное написание).
- `src/mocks/seed/analytics.seed.ts` дополнен данными по 6 новым регионам (Абайская, Жетысуская, Улытауская, Астана, Алматы, Шымкент); Абайская добавлена в `BORDER_REGIONS` (выделена в 2022 г. из приграничной Восточно-Казахстанской, см. допущение в `OPEN_QUESTIONS.md`).
- Атрибуция источника/лицензии продублирована в комментариях `analytics.seed.ts` и `KzHeatMap.tsx`.
- `ComposableMap` center/scale не менялись — географический охват (весь Казахстан) не изменился, только детализация регионов; визуальная сверка по-прежнему не проводилась (headless-браузер недоступен).

**Как проверено:** `npx tsc -b --noEmit`, `npm run build` (бандл даже уменьшился — 951 КБ против 1.1 МБ, благодаря более компактному GeoJSON), `npm run lint` — без ошибок; `npm run smoke:analytics` — подтверждено соответствие 20/20 названий регионов между seed и новым GeoJSON, все остальные проверки данных дашборда по-прежнему проходят.
Вопрос в `OPEN_QUESTIONS.md` (Этап 7) о неактуальности геоданных закрыт с указанием нового источника.

---

## Постфактум-правка №2 — исправление тепловой карты (по скриншоту пользователя после деплоя)

Пользователь задеплоил приложение на Vercel и прислал скриншот: `/admin/dashboard` рендерился сплошным тёмно-синим прямоугольником без единой видимой границы региона — то есть визуальная проверка, недоступная в этом окружении на всех предыдущих этапах, впервые стала возможна, и подтвердила ранее задокументированный риск (`OPEN_QUESTIONS.md`: «проекция/масштаб не проверялись визуально»).

**Сделано:**
1. **`vercel.json`** — добавлен rewrite всех путей на `/index.html`. Без него Vercel возвращал `404 NOT_FOUND` на любой прямой переход/обновление по клиентскому маршруту (например `/admin/login`) — SPA-роутинг целиком на стороне React Router, а Vercel по умолчанию ищет физический файл.
2. **`KzHeatMap.tsx`** — вручную подобранные `center=[67,48]`/`scale=1500` заменены на самовписывающуюся проекцию `geoMercator().fitExtent(...)` (`d3-geo`), которая вычисляет масштаб/центр из фактического bounding box геоданных — устраняет саму категорию проблемы (не нужно вручную «на глаз» подбирать числа без визуальной обратной связи).
3. **Найден и исправлен более серьёзный скрытый баг**, вскрывшийся при диагностике: после конвертации шейпфайла через `mapshaper` порядок обхода вершин (winding order) у **всех 20** полигонов был инвертирован — `d3-geo` интерпретировал каждый регион как «весь земной шар минус сам регион» (`geoArea` ≈ 4π ср, `geoBounds` = вся сфера). Это и есть настоящая причина «сплошного прямоугольника»: даже с идеально подобранным `scale`, карта рендерила бы то же самое, потому что рендерился не Казахстан, а его инверсия. Флаг `mapshaper ... rfc7946` не устранил проблему в установленной версии инструмента — исправлено вручную реверсом порядка точек в каждом кольце координат.
4. `scripts/smoke-heatmap-projection.ts` (`npm run smoke:heatmap`) — новый дымовой тест, который теперь ловит именно этот класс ошибок без браузера: считает пиксельные bbox всех регионов через `d3-geo geoPath.bounds()` и падает, если (а) координаты не число, (б) какой-то регион занимает >90% холста (сигнатура бага winding-инверсии), (в) страна обрезана или, наоборот, не заполняет холст. Добавлен в `smoke:all`.

**Как проверено:** диагностический прогон подтвердил проблему (`geoBounds` всех 20 регионов = `[[-180,-90],[180,90]]`, `geoArea` ≈ 12.56 ср = 4π у каждого — то есть буквально «вся сфера» для каждого региона одновременно, что и объясняет сплошную заливку на скриншоте); после реверса колец — `geoBounds` стали корректными (лон 46.49–87.28°E, лат 40.59–55.43°N, в точности контур Казахстана), `smoke:heatmap` проходит (страна вписывается в холст 800×480 без обрезки, 0 вырожденных регионов). Полная регрессия (`tsc`, `build`, `lint`, `smoke:analytics`) — без ошибок.

**Урок на будущее (зафиксирован в `OPEN_QUESTIONS.md`):** при любой будущей смене источника геоданных для карты — обязательно прогонять `npm run smoke:heatmap`, а не полагаться на успешную сборку: TypeScript и Vite физически не могут поймать некорректную геометрию (JSON синтаксически валиден), инверсию winding-order ловит только сама геометрическая библиотека при вычислении площади/границ. Также это прямое практическое подтверждение риска, о котором предупреждалось во всех предыдущих PROGRESS.md-записях: без визуальной проверки в браузере часть багов остаётся невидимой до первого реального просмотра пользователем — что здесь и произошло.

---

## Постфактум-правка №3 — реальный runtime-баг react-simple-maps (найден по консоли браузера после деплоя)

После правки winding order пользователь передеплоил и всё равно увидел белый экран на `/admin/dashboard`, с ошибкой в консоли хостинга: `TypeError: r is not a function` внутри минифицированного `useMemo`. Попытка воспроизвести локально; сеть в среде временно отсутствовала — пользователь восстановил её и попросил продолжить.

**Диагностика (без слепого исправления):**
- Прочитан исходник `node_modules/react-simple-maps/dist/index.es.js` (`makeProjection`): при передаче функции в проп `projection`, библиотека делает `if (typeof projection === 'function') return projection` — то есть **не вызывает** её как фабрику `(width, height, config) => GeoProjection`, вопреки декларации в `@types/react-simple-maps`. Переданная функция затем используется напрямую как d3-объект проекции: `geoPath().projection(proj)`, и далее `proj([lon, lat])` вызывается для каждой точки геометрии.
- `KzHeatMap.tsx` передавал именно фабрику (`useKzProjection`-функцию вида `(width, height) => GeoProjection`). При вызове `proj([lon, lat])` первым аргументом «шириной» оказывался массив `[lon, lat]`, что и приводило к `TypeError: r is not a function` где-то внутри d3-geo при попытке использовать некорректно сконструированный результат.

**Исправлено:** проекция теперь вычисляется один раз через `useMemo` (`useKzProjection()`) и передаётся в `ComposableMap` уже готовым d3-объектом (не фабрикой) — с `as never`-каст и комментарием, объясняющим расхождение между типами и рантаймом этой версии библиотеки.

**Впервые в проекте — реальная визуальная проверка в браузере:** сеть восстановилась, установлен Playwright + Chromium (через `npm ... playwright install chromium`, ~300 МБ, в отдельной scratch-директории вне репозитория). Написан драйвер-скрипт: логин администратора → `/admin/dashboard` → скриншот + сбор `console`/`pageerror`. Результат: **0 ошибок**, дашборд отрисовался полностью, тепловая карта показывает настоящий узнаваемый контур Казахстана с видимыми границами 20 областей и корректной цветовой шкалой. Дополнительно прогнаны все 10 маршрутов потребительского модуля (мобильная ширина 390px) — тоже 0 ошибок консоли на каждом; скриншотами подтверждён визуально корректный вид экрана выбора канала, входа и списка типов регистрации.
Замеченный на скриншоте графика «СНТ vs Продажи» артефакт (две точки, похожие на слипшуюся «восьмёрку» справа от графика) при проверке через DOM (подсчёт `<circle class="recharts-dot">`, ровно 24 = 12 месяцев × 2 серии, без дублей) оказался не багом — это последние точки обеих серий за «Июл», близкие по значению (индекс легальности к концу периода 94%), визуально слипшиеся в сжатом PNG. Изменений не потребовалось — важный урок: проверять DOM/данные, а не чинить то, что показалось странным на скриншоте.

**Как проверено:** `npx tsc -b --noEmit`, `npm run build`, `npm run lint`, `npm run smoke:all` (все 9 дымовых скриптов) — без ошибок. `scripts/smoke-heatmap-projection.ts` дополнен проверкой именно этого соглашения о вызове: `path.projection()([lon,lat])` должен вернуть `[x,y]` (числа), а не объект/исключение — тест теперь ловит регресс этого класса без браузера в будущем. Playwright-проверка (вне репозитория, разовая) — реальный скриншот дашборда и всех экранов потребительского модуля, 0 ошибок консоли.

**Отклонения/примечания:** Playwright и Chromium установлены во временную scratch-директорию (`%TEMP%/pwdriver`, `%TEMP%/npmcache-scratch`) вне репозитория — не добавлены в зависимости проекта, так как это разовая диагностика в конкретной среде агента, а не часть постоянного тестового стека проекта (браузерное тестирование не входило в объём AGENT_BUILD_PLAN.md). Если нужна регулярная браузерная регрессия — стоит осознанно добавить `@playwright/test` в `devDependencies` отдельной задачей.

---

## Доработка по правкам ПМ — 6 изменений бизнес-логики (обновлённые `JanarmAI_TOR_MVP.docx` / `JanarmAI_Frontend_TechPlan.docx`)

Заказчик внёс 6 точечных изменений в раздел 4 бизнес-ТЗ. Документы в `./data` заменены на обновлённые версии (перечитаны полностью перед началом работы). Ниже — план сопоставления каждого изменения с конкретными файлами кодовой базы, составленный **до** начала правок.

### План сопоставления

| # | Изменение ТЗ | Затронутые файлы |
|---|---|---|
| 1 | eGov+БВУ → один канал | `ChannelSelectPage.tsx` (2 кнопки вместо 3), `EgovBvuRegisterPage.tsx` (убрать различение egov/bvu по `location.state`) |
| 2 | Согласие на ПД переезжает на S-00 | `ChannelSelectPage.tsx` (добавить блок согласия, гейтинг кнопок каналов), `steps/ConsentStep.tsx` (удалить как отдельный шаг визарда — использовался только в `EgovBvuRegisterPage.tsx`), `EgovBvuRegisterPage.tsx` (убрать шаг `'consent'`) |
| 3 | SMS убран из регистрации, остаётся только при входе | `EgovBvuRegisterPage.tsx`, `KmgRegisterPage.tsx` (убрать шаг `'sms'`, `SmsStep` импорт). `steps/SmsStep.tsx` и `mocks/api/identity.ts` (`sendSmsCode`/`verifySmsCode`) — **не трогать**, используются в `LoginPage.tsx` (S-16). `ForeignRegisterPage.tsx` — уже без SMS изначально (только текстовый ввод телефона), проверить и не трогать функционально |
| 4 | ТС+страховка → один запрос к базе МВД | `mocks/api/vehicle.ts` (`checkVehicleRegistry`+`checkOgpoInsurance` → единый `checkMvdRegistry`), `types/mock.ts` (`VEHICLE_NOT_FOUND`+`OGPO_NOT_INSURED` → `MVD_CHECK_FAILED`), `steps/VehicleCheckStep.tsx` (убрать ручной выбор «Да/Нет»+ввод ГРНЗ, сделать авто-степпером как `GbdFlBmgStep`), `mocks/api/index.ts`, `dev/MocksDevPage.tsx`, `scripts/smoke-mocks.ts` (обновить импорты/вызовы). Только ветки ФЛ (4.1–4.3) — автопарк ЮЛ (`VehicleFleetStep.tsx`) эту проверку никогда не использовал, не трогать |
| 5 | Шаг «Выпуск карты» убран → сразу на экран карты/кабинет с уведомлением | `steps/CardIssueStep.tsx` (удалить), все 5 файлов веток регистрации (`EgovBvuRegisterPage.tsx`, `KmgRegisterPage.tsx`, `ForeignRegisterPage.tsx`, `CompanyRegisterPage.tsx`, `CompanyForeignRegisterPage.tsx` — убрать шаг `'card'`, навигация сразу на `/card` (ФЛ) или `/cabinet` (ЮЛ) с `state: { justIssued: true }`), `CardScreenPage.tsx` и `CabinetUlPage.tsx` (добавить баннер-уведомление по `location.state.justIssued`), `registrationActions.ts` (**попутно найденный баг**: `finalizeFlRegistration`/`finalizeUlRegistration` никогда не устанавливали сессию (`login()`) — без этого переход на `/card`/`/cabinet` показывал бы «нет активной сессии»; добавляется автологин сразу после регистрации, что теперь строго необходимо для этого изменения) |
| 6 | КМГ — веб-приложение, вход нерезидентов через этот канал | Уже так реализовано (`ForeignRegisterPage.tsx`/`CompanyForeignRegisterPage.tsx` — только канал `kmg`). Изменений не требуется, сверено ниже |

_план составлен, приступаю к правкам_

### Реализация

**1. eGov+БВУ → один канал.**
- `ChannelSelectPage.tsx`: 3 кнопки (`eGov Mobile` / `Приложение БВУ` / `Приложение КМГ`) заменены на 2 (`Через eGov / банковское приложение` / `Приложение КМГ`). Переход на `/register/egov-bvu` больше не передаёт `state.channel` — маршрут остался тот же один, различение egov/bvu внутри убрано.
- `EgovBvuRegisterPage.tsx`: убрано чтение `location.state.channel` и словарь `CHANNEL_LABEL`, заголовок визарда — фиксированный текст. Внутреннее значение `channel` при регистрации зафиксировано как `'egov'` (допущение, см. `OPEN_QUESTIONS.md`).

**2. Согласие на ПД → на первый экран визарда (уточнено пользователем после первой версии).**
- Первая версия правки разместила согласие на `ChannelSelectPage.tsx` (S-00, буквально по тексту ТЗ 4.0 «Первый экран регистрации совмещает выбор канала и согласие»). Пользователь, увидев результат вживую, уточнил: согласие должно быть не на экране выбора канала, а на первом экране **самого визарда после выбора канала** — там, где для eGov/БВУ уже показаны конкретные данные пользователя (ФИО/ИИН/телефон), а не раньше, когда данных ещё нет.
- Исправлено: `ChannelSelectPage.tsx` возвращён к простому виду без согласия и гейтинга (2 кнопки каналов, без `disabled`). Согласие перенесено в `EgovBvuRegisterPage.tsx` — чекбокс под кнопкой «Продолжить» на шаге `'identity'`, сама кнопка получила `disabled={!consentChecked}`.
- `steps/ConsentStep.tsx` — **удалён** (был нужен только в `EgovBvuRegisterPage.tsx`).
- **Уточнение объёма:** по явному указанию пользователя это исправление применено **только к ветке eGov/БВУ**. Для ветки «Приложение КМГ» (и её подветвей — иностранец, ЮЛ-резидент, ЮЛ-нерезидент) место размещения согласия пользователь оставил на потом («остальные потом если что скажу») — сейчас там согласия нет вообще ни на одном экране (не на S-00, не внутри визарда). Это осознанный временный пробел, а не ошибка — зафиксировано в `OPEN_QUESTIONS.md`, требует отдельного уточнения от пользователя/ПМ, в какой конкретно момент КМГ-визарда (выбор типа лица без единой кнопки «Продолжить», либо после ввода ИИН/ФИО/телефон, либо иначе) должно появиться согласие.

**3. SMS убран из регистрации.**
- `EgovBvuRegisterPage.tsx` и `KmgRegisterPage.tsx`: шаг `'sms'` и импорт `SmsStep` убраны; после `identity`/`gbd-bmg` переход сразу на `'vehicle'`.
- `ForeignRegisterPage.tsx`: проверено — шаг `'phone'` уже был обычным текстовым полем без OTP (соответствовал ещё старому ТЗ 4.3: «для иностранцев... отдельная SMS-верификация не требуется»), функционально не менялся, только уточнён поясняющий текст.
- `steps/SmsStep.tsx`, `mocks/api/identity.ts` (`sendSmsCode`/`verifySmsCode`) — **не тронуты**, используются в `LoginPage.tsx` (S-16, вход по телефону через приложение КМГ) — сверено, что это единственное оставшееся место использования.

**4. ТС+страховка → единый запрос к базе МВД.**
- `mocks/api/vehicle.ts`: `checkVehicleRegistry`+`checkOgpoInsurance` заменены на `checkMvdRegistry(identifier)` → `{ vehicle: {grnz, category} | null; insured: boolean }`. Детерминированное мок-правило (по последней цифре идентификатора) и генерация ГРНЗ без пользовательского ввода — см. `OPEN_QUESTIONS.md`.
- `types/mock.ts`: `VEHICLE_NOT_FOUND`+`OGPO_NOT_INSURED` → единый `MVD_CHECK_FAILED`.
- `steps/VehicleCheckStep.tsx`: полностью переписан — вместо развилки «Да/Нет есть ТС» + формы ввода ГРНЗ теперь автозапускающийся степпер (по образцу `GbdFlBmgStep.tsx`), один вызов `checkMvdRegistry`, кнопка «Повторить проверку» при ошибке.
- `mocks/api/index.ts`, `dev/MocksDevPage.tsx`, `scripts/smoke-mocks.ts` — обновлены импорты/сценарии под новую функцию.
- Автопарк ЮЛ (`VehicleFleetStep.tsx`, `CompanyRegisterPage.tsx`/`CompanyForeignRegisterPage.tsx`) — не тронут, эта проверка там никогда не применялась (там ГРНЗ/категория вводит администратор автопарка вручную по каждому ТС, это отдельная бизнес-логика).

**5. Шаг «Выпуск карты» убран.**
- `steps/CardIssueStep.tsx` — **удалён**.
- Все 5 файлов веток регистрации: шаг `'card'` убран из `STEP_ORDER`; по завершении `LimitResultStep`/`DriverAssignStep` вызывается `finalizeFlRegistration`/`finalizeUlRegistration`, затем `navigate('/card', { state: { justIssued: true } })` (ФЛ: `EgovBvuRegisterPage`, `KmgRegisterPage`, `ForeignRegisterPage`) или `navigate('/cabinet', { state: { justIssued: true } })` (ЮЛ: `CompanyRegisterPage`, `CompanyForeignRegisterPage`).
- `CardScreenPage.tsx`: добавлен дизмиссящийся баннер (иконка + «Карта успешно выпущена» + кнопка «Перейти в кабинет» + крестик закрытия) поверх карты, показывается по `location.state.justIssued`.
- `CabinetUlPage.tsx`: аналогичный баннер («Карты успешно выпущены», с пояснением про по-ТС карты), тоже по `location.state.justIssued`.
- `registrationActions.ts`: **попутно найденный и исправленный баг** — `finalizeFlRegistration`/`finalizeUlRegistration` никогда не вызывали `useUserStore.getState().login(...)`. До этой доработки это маскировалось тем, что после регистрации показывался промежуточный `CardIssueStep` с кнопкой «Перейти в личный кабинет», и баг не был замечен вручную; `smoke-registration.ts` тоже не проверял сессию. Теперь, когда навигация идёт сразу на защищённые сессией `/card`/`/cabinet`, автологин стал строго необходим — добавлен в конец обеих функций.

**6. КМГ — веб-приложение / канал нерезидентов.** Сверено — расхождений с кодом нет, изменений не вносилось (см. `OPEN_QUESTIONS.md`).

### Как проверено

- **Статический анализ:** `npx tsc -b --noEmit`, `npm run build`, `npm run lint` — чисто после каждой партии правок и финально после всех изменений разом.
- **`npm run smoke:all`** (9 скриптов) — без ошибок. `scripts/smoke-registration.ts` дополнен: (а) прямые проверки `checkMvdRegistry` — ошибка на «6», «нет ТС+ОГПО» на «0», «легковая на пользователе» на «4»; (б) проверка автологина — после `finalizeFlRegistration`/`finalizeUlRegistration` `currentUserId`/`currentCompanyId` должны сразу совпадать с созданным пользователем/компанией (тест специально писался так, чтобы упасть, если баг с отсутствующим `login()` вернётся). `scripts/smoke-mocks.ts` обновлён под `checkMvdRegistry` (было: `checkVehicleRegistry`).
- **Впервые в проекте — сквозная проверка изменённого флоу кликами в реальном браузере** (Playwright, окружение см. предыдущую запись PROGRESS.md): dev-сервер (`npm run dev`, порт 5173) + два драйвер-скрипта вне репозитория:
  - Сценарий ФЛ (`/` → согласие → «Через eGov / банковское приложение» → identity → auto-степпер МВД → «Выпустить карту» → `/card`): подтверждено, что кнопки каналов **заблокированы** до согласия и **разблокируются** после; подтверждено отсутствие SMS-шага; подтверждён переход на `/card` с видимым баннером «Карта успешно выпущена». **0 ошибок консоли.**
  - Сценарий ЮЛ (`/` → согласие → «Приложение КМГ» → «ЮЛ · резидент РК» → БИН+ЭЦП (демо-код 1234) → ГБД ЮЛ (авто) → добавление ТС → пропуск водителя → `/cabinet`): подтверждён переход в кабинет ЮЛ с баннером «Карты успешно выпущены», карта нового ТС видна в сводной таблице с прогресс-баром лимита. **0 ошибок консоли.**
  - Оба сценария также явно проверили ключевое для этой доработки: SMS-экран (`text=Код подтверждения отправлен`) **не появляется** ни в одном из путей регистрации.

### Отклонения/примечания

- Три новых допущения (внутреннее значение `channel` для объединённого пути; правило детерминированного мока `checkMvdRegistry`; необходимость автологина) зафиксированы в `OPEN_QUESTIONS.md` с пометкой «закрыт этой доработкой» — они одновременно и отвечают на открытые вопросы, поднятые самой правкой ПМ, и фиксируют решение для будущих сверок.
- Компонент `ChannelCard` не потребовал изменений — существовавший проп `disabled` идеально подошёл для гейтинга согласия, что подтверждает целесообразность его добавления ещё на Этапе 2.
- Размер бандла не изменился существенно (949 КБ, было 951 КБ) — правка чисто структурная (удаление двух шагов, замена двух мок-функций одной), новых зависимостей не добавлялось.

### Постфактум-правка — размещение согласия для eGov/БВУ (по живому фидбэку пользователя)

Пользователь открыл dev-сервер (запущен по его же просьбе), прошёл экран S-00 → «Через eGov / банковское приложение» и прислал скриншот экрана с уже подтянутыми каналом данными (ФИО/ИИН/телефон) и кнопкой «Продолжить», указав, что согласие должно быть здесь, внизу, а не на S-00.

**Сделано:** `ChannelSelectPage.tsx` возвращён к исходному виду без согласия/гейтинга; `EgovBvuRegisterPage.tsx` — чекбокс согласия добавлен под кнопкой «Продолжить» на шаге `'identity'`, кнопка получила `disabled={!consentChecked}`. По прямому указанию пользователя («нет, пока это только для егова остальные потом если что скажу») **изменение применено только к ветке eGov/БВУ** — ветка «Приложение КМГ» и её подветви (иностранец, ЮЛ-резидент, ЮЛ-нерезидент) пока оставлены как есть, без согласия на каком-либо экране. Это осознанный временный пробел по прямому указанию пользователя, отложенный до отдельного уточнения (см. `OPEN_QUESTIONS.md`).

**Как проверено:** `npx tsc -b --noEmit`, `npm run build`, `npm run lint` — чисто. Проверено вживую в браузере (Playwright, тот же dev-сервер на 5173): кнопка канала на S-00 больше не заблокирована; на экране `'identity'` кнопка «Продолжить» заблокирована до отметки чекбокса и разблокируется сразу после — поведение и вид совпадают со скриншотом пользователя. 0 ошибок консоли.
