# JanarmAI — шпаргалка для демо (вся система)

`npm run dev` в `janarmai-front` → **http://localhost:5173**. Всё — мок, без бэкенда, данные в `localStorage`.
**Сбросить демо-данные** — кнопка внизу потребительского модуля и в шапке аналитики (чистит `localStorage`, возвращает seed).

Два независимых модуля: **Потребитель** (`/`) и **Аналитика** (`/admin/login`).

---

## 1. Учётные записи

### Аналитика (`/admin/login`)

| Логин | Пароль | Роль |
|---|---|---|
| `kmg.analyst` | `demo1234` | КМГ |
| `minenergo.analyst` | `demo1234` | Минэнерго |
| `akimat.analyst` | `demo1234` | Акимат |

### Потребитель — вход в готовый аккаунт (`/login`)

| ФИО / компания | Способ входа | Данные | Особое |
|---|---|---|---|
| Асанов Ануар Бахытович | eGov Mobile → выбрать из списка | тап по имени | **2 карты** (легковая + грузовая) |
| Дюсенова Сауле Максатовна | eGov Mobile → выбрать из списка | тап по имени | демо «**только мониторинг**» — без суточного лимита, карта не привязана к ТС |
| Ахметова Гульнара Сериковна | По номеру телефона | `+77011234502`, SMS `1234` | **2 карты** (легковая + грузовая) |
| Smirnov Ivan Petrovich | По номеру телефона | `+79261234567`, SMS `1234` | иностранец, без лимита |
| ТОО «Жолжелдер Логистикс» | Для бизнеса | БИН `123456789012`, код `1234` | ЮЛ-резидент, автопарк 3 ТС |
| ООО «ТрансЛогистик РУ» | Для бизнеса | рег.номер `RU-7743012345`, код `1234` | ЮЛ-нерезидент, автопарк 2 ТС, без лимита |

Универсальный код (SMS / ЭЦП / демо-код) везде — **`1234`**.

---

## 2. Вход/регистрация через eGov — теперь имитация настоящего приложения

Кнопка называется «**Имитация регистрации/входа через eGov**». После клика открывается полноэкранный мок приложения eGov Mobile (свой статус-бар, поиск, баннеры, грид сервисов) — нужно тапнуть плитку **JanarmAI**.

- **Регистрация:** тап по плитке → экран согласия eGov «Разрешить / Запретить» доступ к ФИО/ИИН/телефону → «Разрешить» → обычный визард JanarmAI (данные уже подставлены, чекбокс согласия на ПД + опциональный чекбокс «демо: только мониторинг») → автопроверка МВД (степпер с загрузкой) → карта(ы).
- **Вход:** тап по плитке → сразу загрузка «Выполняется вход через eGov…» (без экрана согласия — eGov уже авторизовал сессию) → сразу в приложении.

---

## 3. Пять сценариев регистрации

| Ветка | Путь | Шаги | Итог |
|---|---|---|---|
| **eGov ФЛ** | `/` → «Имитация регистрации через eGov» | Мок eGov → согласие → данные подставлены → авто-МВД | 1 карта (или 2, если идентификатор оканчивается на «5») |
| **КМГ ФЛ-резидент** | `/register/kmg` → «ФЛ · резидент РК» | Форма ФИО/ИИН/телефон (ручной ввод) → ГБД ФЛ → БМГ → МВД | 1–2 карты по цифре ИИН (см. §6) |
| **ФЛ-иностранец** | → «ФЛ · иностранец» | Паспорт+liveness → Беркут → телефон → МВД | 1 карта, без лимита, предельная цена |
| **ЮЛ-резидент** | → «ЮЛ · резидент РК» | БИН+ФИО+телефон+ЭЦП(`1234`) → ГБД ЮЛ → автопарк (ГРНЗ+категория) → водитель (опц., ИИН) | карта на каждое ТС |
| **ЮЛ-нерезидент** | → «ЮЛ · нерезидент» | Компания вручную → паспорт+liveness+Беркут директора → автопарк | карта на каждое ТС, **без лимита и без льготы** (шага водителя нет) |

Регистрация **сразу логинит** и переносит на `/card` (ФЛ) или `/cabinet` (ЮЛ) с баннером «Карта успешно выпущена» / «Карта выпущена» (мониторинг).

---

## 4. Карта, QR, «заправка»

Тап по QR на `/card` → полноэкранный режим → фазы: **скан → «QR-код принят»**. Пользователь не видит саму проверку лимита — это имитирует работу кассира АЗС. Через ~4–5 сек → «Перейти в историю» → заправка уже там (объём/топливо подобраны автоматически).

**Ключевое:** превышение лимита не блокирует заправку — пересчёт по рыночной (не льготной) цене. Суточный счётчик реально сбрасывается в 00:00 Астаны (фоновая проверка).

**Аккаунт «только мониторинг»** (Дюсенова С.М.): на карте — «Куплено топлива **сегодня**», под картой — «Куплено топлива **всего**»; блока «сброс лимита / цена в пределах лимита» нет (к нему неприменимо).

---

## 5. Кабинет

| Тип | Что видно |
|---|---|
| ФЛ | Карта(ы) с QR, лимит (или объём покупок — для мониторинга), история |
| ЮЛ | Таблица автопарка (ТС + водитель + лимит по каждому), «Добавить ТС», «Назначить водителя», «Скачать отчёт» (CSV по автопарку) |

---

## 6. Модуль «Аналитика»

`/admin/login` → любой аккаунт из §1. В шапке — **две вкладки: «Дашборд» и «Отчёт»**.

Единый блок фильтров (**период / регион / марка топлива / резидентство**) — пересчитывает всё ниже.

### Дашборд

| Блок | Что это |
|---|---|
| Объём по маркам топлива | **6 марок** (АИ-92/95/98/100, ДТ летнее/зимнее), тонны, с дельтой |
| Сверка с СУНП | закуп vs факт, тонны + % + тренд по неделям; ниже — разрыв по топ-5 контрагентам/сетям (цифры теперь согласованы с цифрами сверху) |
| Доля отпуска нерезидентам | по **объёму отпусков** (не по числу лиц), плитка + тренд + донат резиденты/нерезиденты |
| Доля операций сверх лимита | по объёму и по кол-ву операций |
| Тепловая карта по регионам РК | заливка — риск-уровень, размер маркера — объём; клик → боковая панель деталей (не тултип); под картой — **сортируемая таблица** по всем регионам |
| Методология | статичная справка по 4 порогам (не счётчики) |

### Отчёт (новое, по запросу ПМ)

Те же фильтры + **предпросмотр** сводной таблицы по регионам (та же, что под картой, с сортировкой по клику на заголовок) + кнопка **«Скачать отчёт»** — реальный CSV-экспорт текущей выборки.

Риск-тиры везде одной шкалой: **High 80–100 / Medium 50–79 / Low 0–49**. Очередь кейсов (`/admin/cases`) полностью скрыта — 404 по прямому URL.

---

## 7. Мок-логика проверок — триггеры (только там, где ввод ручной)

| Проверка | Триггер | Результат |
|---|---|---|
| ГБД ФЛ | ИИН на `9` / `8` | не найден / ФИО не совпадает |
| БМГ | ИИН на `7` | телефон не привязан |
| МВД (ТС+ОГПО) | ИИН на `6` | ошибка сервиса |
| МВД | `0` | своего ТС нет, но ОГПО есть → легковая 100 л |
| МВД | чётная (кроме `0`/`6`) | легковая 100 л |
| МВД | нечётная, кроме `5` | грузовая 300 л |
| МВД | **`5`** | сразу 2 ТС → **2 карты** |
| ГБД ЮЛ | БИН на `9` | не найден |
| Беркут | паспорт содержит `DUP` / `NF` | дубликат / не подтверждена |
| SMS / ЭЦП | любой код кроме `1234` | неверный код |

---

## 8. Лимиты и цены (`src/lib/cardRules.ts`)

| Кто | Лимит/сутки | Цена | На кого карта |
|---|---|---|---|
| ФЛ-резидент, легковая / без ТС | 100 л | льготная | на человека (не дублируется) |
| ФЛ-резидент, грузовая | 300 л | льготная | на каждое ТС |
| ФЛ-иностранец | без лимита | предельная | 1 карта на человека |
| ЮЛ-резидент | 100/300 л | льготная | на каждое ТС автопарка |
| ЮЛ-нерезидент | без лимита | предельная | на каждое ТС автопарка |
| Демо «только мониторинг» | без лимита | — (не льготная) | 1 карта, без привязки к ТС |

Подробности и открытые вопросы — `OPEN_QUESTIONS.md` / `REGISTRATION_GUIDE.md`. Сценарий питча заказчику — `PM_GUIDE.md`.
