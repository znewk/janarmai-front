# JanarmAI — шпаргалка для демо (вся система)

`npm run dev` в `janarmai-front` → **http://localhost:5173**. Всё — мок, без бэкенда, данные в `localStorage`.
**Сбросить демо-данные** — кнопка внизу кабинета потребителя и в шапке аналитики (чистит `localStorage`, возвращает seed).

Два независимых модуля: **Потребитель** (`/`) и **Аналитика** (`/admin/login`).

---

## 1. Тестовые доступы — всё в одном месте

### Аналитика (`/admin/login`)

| Логин | Пароль | Роль |
|---|---|---|
| `kmg.analyst` | `demo1234` | КМГ |
| `minenergo.analyst` | `demo1234` | Минэнерго |
| `akimat.analyst` | `demo1234` | Акимат |

### Потребитель — вход в готовый аккаунт (`/login`)

| ФИО / компания | Способ входа | Данные | Особое |
|---|---|---|---|
| Асанов Ануар Бахытович | eGov Mobile → выбрать из списка | — (тап по имени) | **2 карты** (легковая+грузовая) |
| Ахметова Гульнара Сериковна | По номеру телефона | `+77011234502`, SMS `1234` | **2 карты** (легковая+грузовая) |
| Smirnov Ivan Petrovich | По номеру телефона | `+79261234567`, SMS `1234` | иностранец, без лимита |
| ТОО «Жолжелдер Логистикс» | Для бизнеса | БИН `123456789012`, код `1234` | ЮЛ-резидент, автопарк 3 ТС |
| ООО «ТрансЛогистик РУ» | Для бизнеса | рег.номер `RU-7743012345`, код `1234` | ЮЛ-нерезидент, автопарк 2 ТС, без лимита |

Универсальный код (SMS / ЭЦП) везде — **`1234`**.

**Внимание:** регистрация через «eGov / банковское приложение» на каждый заход создаёт **новый** аккаунт со случайным ФИО из небольшого пула — при повторных тестах список входа eGov растёт и в нём появляются повторяющиеся имена (это не баг, а накопление тестовых прогонов). Чистится кнопкой «Сбросить демо-данные».

---

## 2. Модуль «Потребитель» — маршруты

| Путь | Экран |
|---|---|
| `/` | Выбор канала регистрации |
| `/register/egov-bvu` | Регистрация eGov/БВУ (ФЛ-резидент, короткий путь) |
| `/register/kmg` | Выбор типа лица → далее 4 ветки |
| `/register/kmg/foreign` | ФЛ-иностранец |
| `/register/kmg/company` | ЮЛ-резидент |
| `/register/kmg/company-foreign` | ЮЛ-нерезидент |
| `/login` | Вход в готовый аккаунт |
| `/home` | Главная (после входа) |
| `/card` | Карта + QR |
| `/cabinet` | Кабинет (ФЛ или ЮЛ — по типу аккаунта) |
| `/cabinet/history` | История заправок |

### 2.1. Пять сценариев регистрации

| Ветка | Путь | Шаги | Итог |
|---|---|---|---|
| **eGov/БВУ ФЛ** | `/` → «Через eGov…» | Данные уже подставлены → согласие на ПД → авто-проверка МВД | 1 карта (или 2, если случайно выпадет идентификатор на «5») |
| **КМГ ФЛ-резидент** | `/register/kmg` → «ФЛ · резидент РК» | Форма ФИО/ИИН/телефон (ручной ввод) → ГБД ФЛ → БМГ → МВД | 1–2 карты по цифре ИИН (см. §4) |
| **ФЛ-иностранец** | → «ФЛ · иностранец» | Паспорт+liveness → Беркут → телефон → МВД | 1 карта, без лимита, предельная цена |
| **ЮЛ-резидент** | → «ЮЛ · резидент РК» | БИН+ФИО+телефон+ЭЦП(`1234`) → ГБД ЮЛ → автопарк (ГРНЗ+категория) → **водитель (опц., ИИН)** | карта на каждое ТС |
| **ЮЛ-нерезидент** | → «ЮЛ · нерезидент» | Компания вручную → паспорт+liveness+Беркут директора → автопарк | карта на каждое ТС, **без лимита и без льготы** (шага водителя нет — ИИН РК не подходит иностранцам) |

Регистрация **сразу логинит** и переносит на `/card` (ФЛ) или `/cabinet` (ЮЛ) с баннером.

### 2.2. Вход (`/login`), 3 способа

| Способ | Как |
|---|---|
| eGov Mobile / БВУ | Список аккаунтов канала → тап по имени → мгновенный вход |
| По телефону | Номер → SMS-код `1234` |
| Для бизнеса | БИН/рег.номер → код ЭЦП `1234` |

### 2.3. Карта, QR, «заправка»

Тап по QR на `/card` → полноэкранный режим → фазы: **скан → подтверждение → «QR-код принят»**.
Пользователь **не видит** саму проверку лимита/отпуска — это имитирует работу кассира АЗС. Через ~4–5 сек → кнопка «Перейти в историю» → заправка уже там (объём/топливо подобраны автоматически по остатку лимита).

**Ключевое:** превышение лимита не блокирует заправку — просто пересчёт по рыночной (не льготной) цене.

### 2.4. Кабинет

| Тип | Что видно |
|---|---|
| ФЛ | Карта(ы) с QR, остаток лимита (прогресс-бар), история |
| ЮЛ | Таблица автопарка (ТС + водитель + лимит по каждому), кнопка «Добавить ТС», «Назначить водителя», «Скачать отчёт» (CSV по всем заправкам автопарка) |

---

## 3. Модуль «Аналитика» — что показывать

`/admin/login` → любой аккаунт из §1 → `/admin/dashboard`.

| Блок | Что это |
|---|---|
| KPI (верх) | Объём реализации, индекс легальности, предотвращённый вывоз, открытые кейсы (High), доля нерезидентов, среднее время реакции — у каждого дельта + мини-график |
| Тепловая карта РК | Заливка по риск-тиру региона (не по объёму), размер точки = объём |
| Рейтинг сетей АЗС | Сверка авторизаций системы с чеками ОФД — расхождение = сигнал (демо: сеть «Гелиос») |
| Классификатор аномалий | Категории: вывоз/контрабанда, превышение лимита, «профессиональный челнок», технические сбои |
| Очередь кейсов (`/admin/cases`) | Список кейсов с риск-баллом, фильтры, пагинация |
| Карточка кейса (`/admin/cases/:id`) | Reason codes, связанные транзакции, смена статуса + заметка аналитика (персистится) |

Риск-тиры везде одной шкалой: **High 80–100 / Medium 50–79 / Low 0–49**.

---

## 4. Мок-логика проверок — шпаргалка по триггерам

Только там, где ввод **ручной** (КМГ ФЛ-резидент, ФЛ-иностранец, ЮЛ-резидент/нерезидент):

| Проверка | Триггер | Результат |
|---|---|---|
| ГБД ФЛ | ИИН оканчивается на `9` | не найден |
| ГБД ФЛ | ИИН оканчивается на `8` | ФИО не совпадает |
| БМГ | ИИН оканчивается на `7` | телефон не привязан |
| МВД (ТС+ОГПО) | ИИН оканчивается на `6` | ошибка сервиса |
| МВД | `0` | своего ТС нет, но ОГПО есть → легковая 100 л |
| МВД | чётная (`2`/`4`) | легковая 100 л |
| МВД | нечётная, кроме 5 (`1`/`3`) | грузовая 300 л |
| МВД | **`5`** | **сразу 2 ТС** (легковая+грузовая) → **2 карты** |
| ГБД ЮЛ | БИН оканчивается на `9` | не найден |
| Беркут | паспорт содержит `DUP` | дубликат паспорта |
| Беркут | паспорт содержит `NF` | личность не подтверждена |
| SMS / ЭЦП | любой код кроме `1234` | неверный код |

---

## 5. Ключевая бизнес-логика выпуска карт (`src/lib/cardRules.ts`)

| Кто | Лимит/сутки | Цена | На кого карта |
|---|---|---|---|
| ФЛ-резидент, легковая / без ТС | 100 л | льготная | на человека (не дублируется) |
| ФЛ-резидент, грузовая | 300 л | льготная | на каждое ТС |
| ФЛ-иностранец | без лимита | предельная | на человека, 1 карта |
| ЮЛ-резидент | 100/300 л | льготная | на каждое ТС автопарка |
| ЮЛ-нерезидент | **без лимита** | предельная | на каждое ТС автопарка |

Подробности и открытые вопросы — в `REGISTRATION_GUIDE.md` / `OPEN_QUESTIONS.md`. Сценарий презентации заказчику — в `PM_GUIDE.md`.
